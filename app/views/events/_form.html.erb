<%= form_for @event do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :title %> <%= f.text_field :title %><br>
  <%= f.label :hold_at %> <%= f.datetime_select :hold_at %><br>
  <%= f.label :capacity %> <%= f.number_field :capacity %><br>
  <%= f.label :location %> <%= f.text_field :location %><br>
  <%= f.label :owner %> <%= f.text_field :owner %><br>
  <%= f.label :description %> <%= f.text_area :description %><br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
